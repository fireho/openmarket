<% content_for :title, "Editando product" %>
<div class="w-full">
  <h1 class="font-bold text-4xl">
    Editando <%= @product.type_name %>: <%= @product.name %>
  </h1>
  <%= render "form" %>
  <div class="mt-4">
    <%= link_to "Voltar", @product, class: "btn btn-secondary" %>
  </div>
</div>
